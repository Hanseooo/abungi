export function Meter({value,max,label,kind='hp'}:{value:number;max:number;label:string;kind?:'hp'|'pp'}){const safe=Math.max(0,Math.min(1,max?value/max:0));return <div className={`meter meter-${kind}`} aria-label={`${label}: ${Math.round(value)} of ${Math.round(max)}`}><div className="meter-track"><span style={{width:`${safe*100}%`}} /></div><strong>{Math.round(value)}<small>/{Math.round(max)}</small></strong></div>;}
